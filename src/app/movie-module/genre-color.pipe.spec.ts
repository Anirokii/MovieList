import { GenreColorPipe } from './genre-color.pipe';

describe('GenreColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreColorPipe();
    expect(pipe).toBeTruthy();
  });
});
